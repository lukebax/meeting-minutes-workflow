# Python for v1

The workflow will be implemented as a Python project for v1. Python fits the expected work: local transcription tooling, transcript parsing, Markdown generation, Word export, file handling, and a command that Codex can run reliably.
