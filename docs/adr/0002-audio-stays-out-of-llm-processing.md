# Audio Stays Out of LLM Processing

The workflow will transcribe meeting recordings locally and will not send audio files to an LLM. Transcript text may be normalized or summarized with an LLM, which keeps the audio path independent of provider file limits and API-key requirements while still allowing LLMs to improve written outputs.
