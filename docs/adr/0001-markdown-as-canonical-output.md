# Markdown as Canonical Output

The workflow will produce Markdown as the canonical form of each summary output, then derive matching Word documents from those Markdown files. This keeps the generated content easy to inspect, compare, and regenerate while still giving colleagues the `.docx` files they expect to review and share.
