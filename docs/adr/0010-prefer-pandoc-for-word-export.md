# Prefer Pandoc for Word Export

The workflow will prefer Pandoc for converting canonical Markdown outputs into matching Word documents, unless implementation research finds a strong reason not to. Pandoc is a mature document conversion tool with strong Markdown and `.docx` support, and the project prioritizes reliable Word output over minimizing setup complexity.
